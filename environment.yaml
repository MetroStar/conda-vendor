name: conda-vendor-dependencies
channels:
  - conda-forge
dependencies:
  - python=3.10.*
  - conda-lock=1.*
  - pip
  # solvers
  - conda=4.12.0
  - mamba
  - micromamba
  # teset dependencies
  - pytest
  - pytest-mock
  - setuptools
  - wheel
   






